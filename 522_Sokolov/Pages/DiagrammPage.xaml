<Page x:Class="_522_Sokolov.Pages.DiagrammPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_522_Sokolov.Pages"
      xmlns:charts="clr-namespace:System.Windows.Forms.DataVisualization.Charting;assembly=System.Windows.Forms.DataVisualization"
      mc:Ignorable="d" 
      Title="DiagrammPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <TextBlock Text="Пользователь:" Style="{StaticResource BaseTextStyle}"/>
            <ComboBox x:Name="CmbUser" DisplayMemberPath="FIO" Style="{StaticResource BaseComboBoxStyle}" Width="150"/>
            <TextBlock Text="Тип диаграммы:" Style="{StaticResource BaseTextStyle}"/>
            <ComboBox x:Name="CmbDiagram" Style="{StaticResource BaseComboBoxStyle}" Width="150" SelectionChanged="UpdateChart"/>
            <Button x:Name="BtnExportExcel" Content="Экспорт в Excel" Style="{StaticResource BaseButtonStyle}" Click="BtnExportExcel_Click"/>
            <Button x:Name="BtnExportWord" Content="Экспорт в Word" Style="{StaticResource BaseButtonStyle}" Click="BtnExportWord_Click"/>
        </StackPanel>
        <WindowsFormsHost Grid.Row="1" Style="{StaticResource BaseWindowsFormsHostStyle}">
            <charts:Chart x:Name="ChartPayments">
                <charts:Chart.Legends>
                    <charts:Legend></charts:Legend>
                </charts:Chart.Legends>
            </charts:Chart>
        </WindowsFormsHost>
    </Grid>
</Page>