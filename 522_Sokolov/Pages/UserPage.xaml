<Page x:Class="_522_Sokolov.Pages.UserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_522_Sokolov.Pages"
      mc:Ignorable="d" 
      Title="UserPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <WrapPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Введите ФИО для поиска:" Style="{StaticResource BaseTextStyle}"/>
            <TextBox x:Name="fioFilterTextBox" Style="{StaticResource BaseTextBoxStyle}" MinWidth="100" TextChanged="fioFilterTextBox_TextChanged"/>
            <TextBlock Text="Сортировка по ФИО:" Style="{StaticResource BaseTextStyle}"/>
            <ComboBox x:Name="sortComboBox" SelectedIndex="0" Style="{StaticResource BaseComboBoxStyle}" SelectionChanged="sortComboBox_SelectionChanged">
                <ComboBoxItem Content="По возрастанию"/>
                <ComboBoxItem Content="По убыванию"/>
            </ComboBox>
            <CheckBox x:Name="onlyAdminCheckBox" Content="Только администраторы" Style="{StaticResource BaseCheckBoxStyle}" Checked="onlyAdminCheckBox_Checked" Unchecked="onlyAdminCheckBox_Unchecked"/>
            <Button x:Name="clearFiltersButton" Content="Очистить фильтр" Style="{StaticResource BaseButtonStyle}" Click="clearFiltersButton_Click_1"/>
        </WrapPanel>
        <ListView Grid.Row="1" x:Name="ListUser" Style="{StaticResource BaseListViewStyle}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="{Binding FIO}" Style="{StaticResource HeaderTextStyle}" TextAlignment="Center"/>
                        <Image Grid.Row="1" Style="{StaticResource BaseImageStyle}">
                            <Image.Source>
                                <Binding Path="Photo">
                                    <Binding.TargetNullValue>
                                        <ImageSource>/522_Sokolov;component/images/default.png</ImageSource>
                                    </Binding.TargetNullValue>
                                </Binding>
                            </Image.Source>
                        </Image>
                        <TextBlock Grid.Row="2" Text="{Binding Role}" Style="{StaticResource BaseTextStyle}" TextAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>